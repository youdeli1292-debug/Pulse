{
  "name": "pulse-xeno-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Node-API bridge that loads the compiled Xeno C++ core (Xeno.dll) into the Pulse Electron shell",
  "main": "build/Release/pulse_xeno.node",
  "gypfile": true,
  "license": "MIT",
  "scripts": {
    "build": "node build.js",
    "build:node": "node-gyp rebuild",
    "clean": "node-gyp clean"
  },
  "devDependencies": {
    "node-gyp": "^11.2.0"
  }
}
